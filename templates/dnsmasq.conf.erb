interface=<%= @accesspoint %>       # Use interface wlan0  
listen-address=<%= @address %>  # Specify the address to listen on  
bind-interfaces      # Bind to the interface
server=<%= @server %>
domain-needed        # Don't forward short names  
bogus-priv           # Drop the non-routed address spaces.  
dhcp-range=<%= @range %> # IP range and lease time  

